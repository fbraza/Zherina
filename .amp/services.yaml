services:
  website:
    command: npm run build -- --devOutput && npm run preview -- --host 0.0.0.0 --port "$PORT" --allowed-hosts
    port: 4321
    health: /
    portal:
      title: Zhirena website
      description: Bundled development preview. Restart the website service after code changes to refresh it.
