<section class="px-4 py-16 md:px-9 lg:min-h-[calc(100svh-72px)] lg:pt-32 lg:pb-20">
    <div
        class="mx-auto grid w-full max-w-[1920px] grid-cols-1 gap-12 lg:grid-cols-[minmax(0,44fr)_minmax(0,56fr)] lg:items-center lg:gap-12"
    >
        <div class="lg:translate-y-[20px] lg:pt-24">
            <h1
                class="max-w-[11ch] font-sans text-[clamp(3rem,5vw,4.5rem)] leading-none tracking-[-0.04em] text-text"
            >
                Turn Science <span class="block">Into Strategy</span>
            </h1>

            <div class="mt-6 max-w-[58ch]">
                <p
                    class="font-mono text-[16px] leading-[1.5] tracking-[-0.01em] text-muted"
                >
                    I love to partner with founders to accelerate the path from
                    bench to market. I will dive into your research and
                    pre-clinical strategy to build investor-ready narratives. I
                    am passionate about science and want it to reach patients.
                </p>
                <p class="mt-4 font-mono text-[13px] leading-none tracking-[0.02em] text-soft">
                    Let's build together.
                </p>
            </div>

            <a
                href="#contact"
                class="hero-cta group mt-10 inline-flex min-h-11 items-center gap-3 border border-rule bg-raised px-4 font-mono text-[12px] leading-none tracking-[0.05em] text-text no-underline uppercase"
            >
                Book a strategy call <span aria-hidden="true">→</span>
            </a>
        </div>

        <div
            class="flex flex-col overflow-hidden rounded-[4px] border border-rule bg-canvas lg:aspect-[4/3]"
            aria-label="Empty workspace window"
            role="img"
        >
            <div class="flex h-11 items-center gap-1.5 border-b border-rule bg-surface px-4">
                <span class="h-2.5 w-2.5 rounded-full bg-[#ff5f57]"></span>
                <span class="h-2.5 w-2.5 rounded-full bg-[#febc2e]"></span>
                <span class="h-2.5 w-2.5 rounded-full bg-[#28c840]"></span>
            </div>
            <div class="min-h-[320px] flex-1 bg-canvas lg:min-h-0"></div>
        </div>
    </div>
</section>

<style>
    .hero-cta {
        position: relative;
        overflow: hidden;
    }

    .hero-cta::before {
        position: absolute;
        inset: 0;
        z-index: 0;
        content: "";
        opacity: 0;
        background: repeating-linear-gradient(
            45deg,
            transparent 0 2px,
            #4d4947 2px 3px,
            transparent 3px 5px
        );
        background-size: 7px 7px;
        transition: opacity 150ms ease-out;
    }

    .hero-cta:hover::before,
    .hero-cta:focus-visible::before {
        opacity: 0.45;
    }

    .hero-cta > * {
        position: relative;
        z-index: 1;
    }
</style>
